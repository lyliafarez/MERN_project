export default class Registration {
    
    userId: string;
    eventId: string;

    constructor(userId: string, eventId: string){
        this.userId = userId;
        this.eventId = eventId;
        
    }
}